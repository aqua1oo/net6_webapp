@page
<h2>Login</h2>

@using (Html.BeginForm("Login", "Account", FormMethod.Post, new { name = "loginForm", id = "loginForm"}))
{
    <div>
        <input type="hidden" name="site_id" value="www" />
        <input type="hidden" name="return_url" value="/" />
        <div class="container login">
            <!-- [s]contnet -->
            <div class="content">
                <div class="login_box">
                    <h2>교원 더 퍼스트 러닝센터<span>회원전용 서비스</span></h2>
                    <p>회원인증 후 온라인 학습을 할 수 있습니다.<br>수학, 영어, 전과목, 한자 회원은 로그인을 해주세요.</p>
                    <div class="form">
                        <input type="text" name="user_id" id="user_id" placeholder="아이디">
                        <input type="password" name="user_pass" id="user_pass" placeholder="비밀번호">
                        <button onclick="fnCheckSubmit();" title="로그인" class="btn_login">로그인</button>
                    </div>
                    <p>로그인이 안될 경우 각 학원에 문의해 주세요.</p>
                </div>
            </div>
            <!-- [e]contnet -->
        </div>
    </div>
}

<script>
    function fnCheckSubmit() {
        $("#loginForm").submit();
    }
</script>